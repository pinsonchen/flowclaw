{
  "name": "flowclaw",
  "private": true,
  "version": "0.1.0",
  "description": "跨平台 AI 工作流引擎",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
